export type stepDetailType = {
  name: string;
  title: string;
  markedWord: string;
  descriptions: string[];
  startUrl: string;
  demoUrl: string;
  photo: string;
};

export type stepInfoType = {
  order: number;
  title: string;
  desc: string;
  url?: string;
};
